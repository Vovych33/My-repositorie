#!/bin/bash
# Определяет чётное или нечётное число

read -p "Введите целое число: " num

if ! [[ "$num" =~ ^-?[0-9]+$ ]]; then
    echo "Ошибка: введите целое число!"
    exit 1
fi

if (( num % 2 == 0 )); then
    echo "Число $num — чётное ✅"
else
    echo "Число $num — нечётное ❌"
fi